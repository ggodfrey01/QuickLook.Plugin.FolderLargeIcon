<UserControl x:Class="QuickLook.Plugin.HelloWorld.Views.FolderGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:QuickLook.Plugin.HelloWorld.ViewModels"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <ListView x:Name="List"
              BorderThickness="0"
              Background="Transparent"
              ScrollViewer.CanContentScroll="False"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="6"/>
                    <Setter Property="Margin" Value="6"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Top"/>
                    <Setter Property="Focusable" Value="False"/>
                </Style>
            </ListView.ItemContainerStyle>

            <!-- IMPORTANT: vm mapping is at the root, not inside the template -->
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:FolderItemViewModel}">
                    <StackPanel Width="168">
                        <Border Width="128" Height="128" CornerRadius="6" BorderThickness="1" Opacity="0.98">
                            <Image Width="128" Height="128" Stretch="Uniform" Source="{Binding Thumbnail}" />
                        </Border>
                        <TextBlock Margin="0,8,0,0"
                       Text="{Binding Name}"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       TextTrimming="CharacterEllipsis"
                       Height="36"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ScrollViewer>
</UserControl>